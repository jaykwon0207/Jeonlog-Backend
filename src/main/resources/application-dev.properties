server.port=8080

# DB (Dev)
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/jeonlog_dev?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useUnicode=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true

# Redis (별도 서버, 지표 수집/조회용)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=2s
spring.data.redis.lettuce.pool.max-active=16
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=2

# Google OAuth
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://openidconnect.googleapis.com/v1/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.redirect-uri=http://3.38.114.252:8080/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=profile,email
google.mobile.allowed-client-ids=${GOOGLE_MOBILE_ALLOWED_CLIENT_IDS:}

# Naver OAuth
spring.security.oauth2.client.provider.naver.authorization-uri=https://nid.naver.com/oauth2.0/authorize
spring.security.oauth2.client.provider.naver.token-uri=https://nid.naver.com/oauth2.0/token
spring.security.oauth2.client.provider.naver.user-info-uri=https://openapi.naver.com/v1/nid/me
spring.security.oauth2.client.provider.naver.user-name-attribute=response

spring.security.oauth2.client.registration.naver.client-id=${NAVER_CLIENT_ID}
spring.security.oauth2.client.registration.naver.client-secret=${NAVER_CLIENT_SECRET}
spring.security.oauth2.client.registration.naver.redirect-uri=http://3.38.114.252:8080/login/oauth2/code/naver
spring.security.oauth2.client.registration.naver.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.naver.scope=name,email
spring.security.oauth2.client.registration.naver.provider=naver

# S3
spring.cloud.aws.region.static=${AWS_REGION}
spring.cloud.aws.s3.bucket=${AWS_BUCKET}
aws.access-key=${AWS_ACCESS_KEY}
aws.secret-key=${AWS_SECRET_KEY}

spring.config.import=optional:file:./.env.dev.properties

cloudfront.domain=${CLOUDFRONT_DOMAIN:}

firebase.credentials.path=${FIREBASE_CREDENTIALS_PATH:}

# App review login
review-login.enabled=${REVIEW_LOGIN_ENABLED:false}
review-login.code=${REVIEW_LOGIN_CODE:}
review-login.oauth-id=${REVIEW_LOGIN_OAUTH_ID:app-reviewer}
review-login.name=${REVIEW_LOGIN_NAME:App Reviewer}

moderation.discord.webhook-url=${MODERATION_DISCORD_WEBHOOK_URL:}
moderation.discord.report-webhook-url=${MODERATION_DISCORD_REPORT_WEBHOOK_URL:}
block.discord.webhook-url=${BLOCK_DISCORD_WEBHOOK_URL:}
